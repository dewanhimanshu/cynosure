<!DOCTYPE html>
<html>
  <head>
    <title>Cynosure</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>


  </head>

  <body>
 
  <% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>"><%= value %></div>
  <% end %>

    <%= yield %>
  </body>
</html>
